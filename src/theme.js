// Design tokens for JS usage (keeps values in sync with CSS variables / Tailwind config)
// Use these constants in components if you need to reference colors programmatically.

export const tokens = {
  colors: {
    accentStart: '#7c3aed',
    accentEnd: '#06b6d4',
    muted: '#64748b',
  },
  radii: {
    roundedLg: '1rem',
  },
}

export default tokens
